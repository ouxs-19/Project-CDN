version: '3.8'

services:

  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:  
      - '4000:4000'
    image: ouxs19/project2cs:backend
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    environment:
      - ENDPOINT_IP=10.0.10.157
      - ENDPOINT_PORT=80